version: "3.9"

services:
  postgres-db:
    image: postgres:16
    container_name: postgres
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: 123456
    ports:
      - "5432:5432"
    networks:
      - postgres-network-db

networks:
  postgres-network-db:
    driver: bridge